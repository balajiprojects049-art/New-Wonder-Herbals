{
    "name": "new-wonder-herbals",
    "version": "1.0.0",
    "scripts": {
        "build": "cd frontend && npm install && npm run build"
    },
    "dependencies": {
        "pg": "^8.11.3"
    }
}